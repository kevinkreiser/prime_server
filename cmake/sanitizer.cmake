if (ENABLE_SANITIZERS)
  set(ENABLE_ADDRESS_SANITIZER ON)
  set(ENABLE_UNDEFINED_SANITIZER ON)
endif()

set (SANITIZER_FLAGS_LIST "")
set (SANITIZER_EXE_LINKER_FLAGS_LIST "")
set (SANITIZER_SHARED_LINKER_FLAGS_LIST "")

if(ENABLE_ADDRESS_SANITIZER)
  message(STATUS "Enabling address sanitizer (ASan).")
  set(OLD_CMAKE_REQUIRED_FLAGS ${CMAKE_REQUIRED_FLAGS})
  set(CMAKE_REQUIRED_FLAGS "${CMAKE_REQUIRED_FLAGS} -fsanitize=address")
  list(APPEND SANITIZER_FLAGS_LIST -fsanitize=address -fno-omit-frame-pointer -fno-optimize-sibling-calls)
  list(APPEND SANITIZER_EXE_LINKER_FLAGS_LIST -fsanitize=address)
  list(APPEND SANITIZER_SHARED_LINKER_FLAGS_LIST -fsanitize=address)
  set(CMAKE_REQUIRED_FLAGS ${OLD_CMAKE_REQUIRED_FLAGS})
endif()

if(ENABLE_UNDEFINED_SANITIZER)
  message(STATUS "Enabling undefined behavior sanitizer (UBSan).")
  list(APPEND SANITIZER_FLAGS_LIST -fsanitize=undefined -fno-sanitize=vptr)
  list(APPEND SANITIZER_EXE_LINKER_FLAGS_LIST -fsanitize=undefined -fno-sanitize=vptr)
  list(APPEND SANITIZER_SHARED_LINKER_FLAGS_LIST -fsanitize=undefined -fno-sanitize=vptr)
endif()

string(REPLACE ";" " " SANITIZER_FLAGS_STR "${SANITIZER_FLAGS_LIST}")
string(REPLACE ";" " " SANITIZER_EXE_LINKER_FLAGS_STR "${SANITIZER_EXE_LINKER_FLAGS_LIST}")
string(REPLACE ";" " " SANITIZER_SHARED_LINKER_FLAGS_STR "${SANITIZER_SHARED_LINKER_FLAGS_LIST}")

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${SANITIZER_FLAGS_STR}")
set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${SANITIZER_EXE_LINKER_FLAGS_STR}")
set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${SANITIZER_SHARED_LINKER_FLAGS_STR}")
